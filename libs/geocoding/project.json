{
  "name": "geocoding",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "geocoding/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project geocoding --web",
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/geocoding/jest.config.ts"
      }
    }
  }
}
